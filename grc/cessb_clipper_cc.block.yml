# auto-generated by grc.converter

id: cessb_clipper_cc
label: Magnitude Clipper
category: '[CESSB]'

parameters:
-   id: clip
    label: Clipping Level
    dtype: float
    default: '1.0'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import cessb
    make: cessb.clipper_cc(${clip})

file_format: 1
